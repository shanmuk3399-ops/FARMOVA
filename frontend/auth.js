document.addEventListener("DOMContentLoaded",function(){const page=location.pathname.split("/").pop()||"index.html";const user=getUser();const protectedRoles={"add-produce.html":"FARMER","farmer-dashboard.html":"FARMER","earnings-page.html":"FARMER","ai-page.html":"FARMER","buyer-dashboard.html":"BUYER","orders.html":"BUYER","fpo-page.html":"FPO"};const requiredRole=protectedRoles[page];if(requiredRole){if(!user){location.href="login.html?next="+encodeURIComponent(page);return;}if(user.role!==requiredRole){alert("This page is for "+requiredRole+" users. Your account is "+user.role+".");location.href="index.html";return;}}document.querySelectorAll("[data-logout]").forEach(btn=>btn.addEventListener("click",logout));const nav=document.querySelector("[data-auth-nav]");if(nav){if(user){nav.innerHTML=`<span class="text-xs text-emerald-400">${escapeAuth(user.name||"User")} • ${escapeAuth(user.role||"")}</span><a href="profile.html" class="text-xs text-slate-300 border border-slate-600 px-3 py-1.5 rounded-lg">Profile</a><button data-logout class="text-xs text-slate-300 border border-slate-600 px-3 py-1.5 rounded-lg">Logout</button>`;}else{nav.innerHTML='<a href="login.html" class="text-xs text-emerald-400 border border-emerald-500/40 px-3 py-1.5 rounded-lg">Login</a><a href="register.html" class="text-xs text-slate-300 border border-slate-600 px-3 py-1.5 rounded-lg">Register</a>';}nav.querySelector("[data-logout]")?.addEventListener("click",logout);}document.querySelectorAll("[data-role-only]").forEach(el=>{const allowed=el.dataset.roleOnly.split(",").map(x=>x.trim().toUpperCase());if(user&&allowed.includes(String(user.role||"").toUpperCase())){el.classList.remove("hidden");}else{el.classList.add("hidden");}});document.querySelectorAll("a[data-protected-page]").forEach(link=>{const target=link.dataset.protectedPage;if(user&&protectedRoles[target]&&protectedRoles[target]===user.role){link.classList.remove("hidden");}else if(protectedRoles[target]){link.classList.add("hidden");}});const portalGrid=document.getElementById("portalGrid");if(portalGrid){const visible=[...portalGrid.children].filter(el=>!el.classList.contains("hidden"));portalGrid.classList.toggle("md:grid-cols-1",visible.length===1);portalGrid.classList.toggle("md:grid-cols-2",visible.length!==1);}const featureGrid=document.getElementById("featureGrid");if(featureGrid){const visible=[...featureGrid.children].filter(el=>!el.classList.contains("hidden"));featureGrid.classList.toggle("lg:grid-cols-1",visible.length===1);featureGrid.classList.toggle("lg:grid-cols-2",visible.length===2);featureGrid.classList.toggle("lg:grid-cols-3",visible.length===3);featureGrid.classList.toggle("lg:grid-cols-4",visible.length>=4);}const loginForm=document.getElementById("loginForm");if(loginForm){const status=document.getElementById("loginStatus"),btn=document.getElementById("loginBtn"),phoneInput=document.getElementById("phone"),passwordInput=document.getElementById("password");loginForm.addEventListener("submit",async function(e){e.preventDefault();if(btn)btn.disabled=true;if(status){status.textContent="Connecting to Farmova...";status.className="text-xs text-center text-slate-400";}try{const data=await apiPost("/auth/login",{phone:phoneInput.value.trim(),password:passwordInput.value});setAuthSession(data);const next=new URLSearchParams(window.location.search).get("next");window.location.href=next||({"FARMER":"farmer-dashboard.html","BUYER":"buyer-dashboard.html","FPO":"fpo-page.html"}[data.user.role]||"index.html");}catch(err){if(status){status.textContent=err.message||"Farmova could not sign you in. Please check your details and try again.";status.className="text-xs text-center text-rose-400";}}finally{if(btn)btn.disabled=false;}});}const registerForm=document.getElementById("registerForm");if(registerForm){const status=document.getElementById("registerStatus"),btn=document.getElementById("registerBtn"),nameInput=document.getElementById("name"),phoneInput=document.getElementById("phone"),passwordInput=document.getElementById("password"),roleInput=document.getElementById("role"),locationInput=document.getElementById("location");registerForm.addEventListener("submit",async function(e){e.preventDefault();if(btn)btn.disabled=true;if(status){status.textContent="Creating your Farmova account...";status.className="text-xs text-center text-slate-400";}try{const name=nameInput.value.trim(),phone=phoneInput.value.trim(),password=passwordInput.value,role=roleInput.value,location=locationInput.value.trim();if(name.length<2)throw new Error("Name must contain at least 2 characters.");if(phone.length<5)throw new Error("Enter a valid phone number.");if(password.length<4)throw new Error("Password must contain at least 4 characters.");if(!role)throw new Error("Please select a role.");const data=await apiPost("/auth/register",{name,phone,password,role,location,language:"English"});if(status){status.textContent=data.message||"Registration successful. Redirecting to login...";status.className="text-xs text-center text-emerald-400";}setTimeout(()=>window.location.href="login.html",900);}catch(err){if(status){status.textContent=err.message||"Farmova could not create the account. Please try again.";status.className="text-xs text-center text-rose-400";}}finally{if(btn)btn.disabled=false;}});}});
async function logout(){try{if(getToken())await apiPost("/auth/logout");}catch(e){console.warn(e);}finally{clearAuthSession();location.href="login.html";}}
function escapeAuth(x){return String(x??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#039;"}[m]));}
