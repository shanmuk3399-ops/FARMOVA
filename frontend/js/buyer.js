let allProduce=[],filteredProduce=[],selectedProduce=null,buyerOrders=[],buyerOffers=[];
document.addEventListener("DOMContentLoaded",function(){const user=getUser();if(!user||user.role!=="BUYER"){if(!user)location.href="login.html?next=buyer-dashboard.html";return;}document.getElementById("searchInput")?.addEventListener("input",applyFilters);document.getElementById("locationSelect")?.addEventListener("change",applyFilters);document.getElementById("sortSelect")?.addEventListener("change",applyFilters);document.getElementById("offerForm")?.addEventListener("submit",submitOffer);document.getElementById("closeDetailsBtn")?.addEventListener("click",closeDetailsModal);document.getElementById("closeOfferBtn")?.addEventListener("click",closeOfferModal);loadVoiceSearch();fetchMarketplaceItems();loadBuyerStatus();});
async function fetchMarketplaceItems(){const grid=document.getElementById("marketplaceGrid");try{grid.innerHTML='<div class="col-span-full text-center py-12 text-slate-400 text-sm">Loading live crop listings...</div>';const data=await apiGet("/produce/list");allProduce=Array.isArray(data)?data:[];populateLocations();applyFilters();}catch(e){console.error(e);if(grid)grid.innerHTML=`<div class="col-span-full text-center py-12 text-rose-400 text-sm">⚠️ ${escapeHtml(e.message||"Could not load marketplace")}</div>`;}}
function populateLocations(){const s=document.getElementById("locationSelect");if(!s)return;const old=s.value,set=new Set();allProduce.forEach(x=>x.location&&set.add(String(x.location).trim()));s.innerHTML='<option value="">All Locations</option>'+[...set].sort().map(x=>`<option value="${escapeAttr(x)}">${escapeHtml(x)}</option>`).join("");if([...s.options].some(o=>o.value===old))s.value=old;}
function normalize(v){return String(v??"").toLowerCase().trim();}
function applyFilters(){const q=normalize(document.getElementById("searchInput")?.value),loc=normalize(document.getElementById("locationSelect")?.value),sort=document.getElementById("sortSelect")?.value||"";filteredProduce=allProduce.filter(x=>(!q||normalize(x.crop_name).includes(q))&&(!loc||normalize(x.location)===loc)&&Number(x.quantity)>0);if(sort==="price_asc")filteredProduce.sort((a,b)=>Number(a.expected_price)-Number(b.expected_price));else if(sort==="price_desc")filteredProduce.sort((a,b)=>Number(b.expected_price)-Number(a.expected_price));else if(sort==="qty_desc")filteredProduce.sort((a,b)=>Number(b.quantity)-Number(a.quantity));else filteredProduce.sort((a,b)=>Number(b.id||0)-Number(a.id||0));renderMarketplace();}
function renderMarketplace(){const grid=document.getElementById("marketplaceGrid");if(!grid)return;if(!filteredProduce.length){grid.innerHTML='<div class="col-span-full text-center py-12 text-slate-400 text-sm">No matching produce found.</div>';return;}grid.innerHTML=filteredProduce.map(item=>`<article class="bg-slate-800 border border-slate-700 rounded-xl p-5 shadow-xl"><div class="flex justify-between gap-3"><div><h3 class="font-bold text-slate-100">${escapeHtml(item.crop_name)}</h3><p class="text-xs text-slate-400 mt-1">📍 ${escapeHtml(item.location||"Not specified")}</p></div><span class="text-xs text-emerald-400 font-bold">₹${Number(item.expected_price||0).toLocaleString("en-IN")}/Qtl</span></div><div class="grid grid-cols-2 gap-3 my-4 text-xs"><div class="bg-slate-900 rounded-lg p-3"><span class="text-slate-400 block">Available</span><b>${item.quantity} ${escapeHtml(item.unit||"Qtl")}</b></div><div class="bg-slate-900 rounded-lg p-3"><span class="text-slate-400 block">Quality</span><b>${escapeHtml(item.quality_grade||"—")}</b></div></div><div class="flex gap-2"><button onclick="showDetails(${Number(item.id)})" class="flex-1 bg-slate-700 hover:bg-slate-600 rounded-lg py-2 text-xs font-bold">VIEW DETAILS</button><button onclick="openOffer(${Number(item.id)})" class="flex-1 bg-emerald-500 hover:bg-emerald-600 text-slate-950 rounded-lg py-2 text-xs font-bold">MAKE OFFER</button></div><button onclick="buyNow(${Number(item.id)})" class="w-full mt-2 bg-blue-500 hover:bg-blue-600 text-white rounded-lg py-2 text-xs font-bold">BUY NOW</button></article>`).join("");}
function showDetails(id){const item=allProduce.find(x=>Number(x.id)===Number(id));if(!item)return;selectedProduce=item;document.getElementById("detailsTitle").textContent=item.crop_name;document.getElementById("detailsContent").innerHTML=`<div class="space-y-3 text-sm"><p><b>Location:</b> ${escapeHtml(item.location||"Not specified")}</p><p><b>Quantity:</b> ${Number(item.quantity||0).toLocaleString("en-IN")} ${escapeHtml(item.unit||"Qtl")}</p><p><b>Quality:</b> ${escapeHtml(item.quality_grade||"—")}</p><p><b>Expected price:</b> ₹${Number(item.expected_price||0).toLocaleString("en-IN")} / Qtl</p><button onclick="openOffer(${Number(item.id)});closeDetailsModal()" class="w-full bg-emerald-500 text-slate-950 font-bold py-2 rounded-lg text-xs">MAKE OFFER</button><button onclick="buyNow(${Number(item.id)});closeDetailsModal()" class="w-full bg-blue-500 text-white font-bold py-2 rounded-lg text-xs">BUY NOW</button></div>`;document.getElementById("detailsModal")?.classList.remove("hidden");document.getElementById("detailsModal")?.classList.add("flex");}
function closeDetailsModal(){const m=document.getElementById("detailsModal");m?.classList.add("hidden");m?.classList.remove("flex");}
function openOffer(id){selectedProduce=allProduce.find(x=>Number(x.id)===Number(id));if(!selectedProduce)return;document.getElementById("offerCropName").textContent=selectedProduce.crop_name;document.getElementById("offerCropInfo").textContent=`Available: ${selectedProduce.quantity} ${selectedProduce.unit||"Qtl"} • Expected: ₹${Number(selectedProduce.expected_price||0).toLocaleString("en-IN")}/Qtl`;document.getElementById("buyerName").value=getUser()?.name||"";document.getElementById("offerPrice").value="";document.getElementById("offerQuantity").value=Math.min(1,Number(selectedProduce.quantity||0));document.getElementById("offerStatus").textContent="";const m=document.getElementById("offerModal");m?.classList.remove("hidden");m?.classList.add("flex");}
function closeOfferModal(){const m=document.getElementById("offerModal");m?.classList.add("hidden");m?.classList.remove("flex");}
async function submitOffer(e){e.preventDefault();if(!selectedProduce)return;const price=document.getElementById("offerPrice").valueAsNumber,qty=document.getElementById("offerQuantity").valueAsNumber,status=document.getElementById("offerStatus");if(!Number.isFinite(price)||price<=0||!Number.isFinite(qty)||qty<=0||qty>Number(selectedProduce.quantity)){status.textContent="Enter a valid offer price and quantity.";status.className="text-xs text-center text-rose-400";return;}try{const data=await apiPost("/offers/place",null,{produce_id:selectedProduce.id,quantity:qty,offered_price:price});status.innerHTML=`Offer submitted: <b>₹${Number(price).toLocaleString("en-IN")}/Qtl</b> • ${qty} ${escapeHtml(selectedProduce.unit||"Qtl")}.`;status.className="text-xs text-center text-emerald-400";await loadBuyerStatus();setTimeout(closeOfferModal,1200);}catch(err){status.textContent=err.message;status.className="text-xs text-center text-rose-400";}}
async function buyNow(id){const item=allProduce.find(x=>Number(x.id)===Number(id));if(!item)return;const raw=prompt(`Quantity to order (${item.unit||"Qtl"}, max ${item.quantity}):`,Math.min(1,Number(item.quantity||0)));const qty=Number(raw);if(!Number.isFinite(qty)||qty<=0||qty>Number(item.quantity||0)){alert("Invalid quantity.");return;}try{const data=await apiPost("/orders/place",null,{produce_id:item.id,quantity:qty});alert(`Order #${data.order_id} placed. Total: ₹${Number(data.total_price||0).toLocaleString("en-IN")}`);await loadBuyerStatus();await fetchMarketplaceItems();}catch(err){alert(err.message);}}
async function loadBuyerStatus(){const box=document.getElementById("buyerStatusList"),user=getUser();if(!box||!user)return;try{const [offerData,orderData]=await Promise.all([apiGet(`/offers/buyer/${user.id}`),apiGet(`/orders/buyer/${user.id}`)]);buyerOffers=Array.isArray(offerData)?offerData:(offerData?.offers||[]);buyerOrders=Array.isArray(orderData)?orderData:[];const byOffer=new Map(buyerOrders.filter(o=>o.offer_id).map(o=>[Number(o.offer_id),o]));const rows=[];buyerOffers.forEach(o=>rows.push({kind:"OFFER",offer:o,order:byOffer.get(Number(o.offer_id))||null,time:o.created_at||""}));buyerOrders.filter(o=>o.source_type!=="OFFER"&&!o.offer_id).forEach(o=>rows.push({kind:"DIRECT",order:o,time:o.created_at||""}));rows.sort((a,b)=>new Date(b.time||0)-new Date(a.time||0));box.innerHTML=rows.length?rows.map(renderBuyerStatus).join(""):'<div class="text-xs text-slate-500">No bids or orders yet. Use MAKE OFFER or BUY NOW.</div>';}catch(e){box.innerHTML=`<div class="text-xs text-rose-400">${escapeHtml(e.message||"Could not load your orders")}</div>`;}}
function renderBuyerStatus(x){if(x.kind==="OFFER"){const o=x.offer,ord=x.order;if(String(o.status).toUpperCase()==="REJECTED")return `<div class="bg-slate-900 border border-rose-500/30 rounded-xl p-4"><div class="flex justify-between gap-2"><div><b>${escapeHtml(o.crop_name||"Produce")}</b><p class="text-xs text-slate-400 mt-1">Bidding Offer #${o.offer_id}</p></div><span class="text-xs text-rose-400 font-bold">REJECTED</span></div><p class="text-xs mt-3">Your offer: <b class="text-emerald-400">₹${Number(o.offered_price||0).toLocaleString("en-IN")}/Qtl</b> • ${o.quantity} ${escapeHtml(o.unit||"Qtl")}</p></div>`;const st=ord?(ord.tracking_status||ord.status||"ACCEPTED"):"PENDING";return `<div class="bg-slate-900 border border-slate-700 rounded-xl p-4"><div class="flex justify-between gap-2"><div><b>${escapeHtml(o.crop_name||"Produce")}</b><p class="text-xs text-slate-400 mt-1">Bidding Offer #${o.offer_id} • ${o.quantity} ${escapeHtml(o.unit||"Qtl")}</p></div><span class="text-xs font-bold ${st==="PENDING"?"text-amber-400":"text-emerald-400"}">${escapeHtml(st)}</span></div><p class="text-xs mt-3">Your offer price: <b class="text-emerald-400">₹${Number(o.offered_price||0).toLocaleString("en-IN")}/Qtl</b></p>${ord&&st==="ACCEPTED"?`<button onclick="acceptOrder(${Number(ord.id)})" class="mt-3 w-full bg-emerald-500 hover:bg-emerald-600 text-slate-950 font-bold py-2 rounded-lg text-xs">ACCEPT ORDER</button>`:ord?`<p class="text-xs text-slate-500 mt-3">Order #${ord.id} is ${escapeHtml(st.toLowerCase())}.</p>`:'<p class="text-xs text-slate-500 mt-3">Waiting for the farmer to accept your bid.</p>'}</div>`;}const o=x.order,st=o.tracking_status||o.status||"ACCEPTED";return `<div class="bg-slate-900 border border-slate-700 rounded-xl p-4"><div class="flex justify-between gap-2"><div><b>${escapeHtml(o.crop_name||"Produce")}</b><p class="text-xs text-slate-400 mt-1">Direct Order #${o.id} • ${o.quantity} ${escapeHtml(o.unit||"Qtl")}</p></div><span class="text-xs text-emerald-400 font-bold">${escapeHtml(st)}</span></div><p class="text-xs mt-3">Total: <b class="text-emerald-400">₹${Number(o.total_price||0).toLocaleString("en-IN")}</b></p>${st==="ACCEPTED"?`<button onclick="acceptOrder(${Number(o.id)})" class="mt-3 w-full bg-emerald-500 hover:bg-emerald-600 text-slate-950 font-bold py-2 rounded-lg text-xs">ACCEPT ORDER</button>`:`<p class="text-xs text-slate-500 mt-3">Order is being processed through My Orders tracking.</p>`}</div>`;}
window.acceptOrder=async function(orderId){try{await apiPut(`/orders/${orderId}/status`,null,{status:"PROCESSING"});await loadBuyerStatus();alert("Order accepted and moved to Processing.");}catch(e){alert(e.message||"Could not accept order");}};
function loadVoiceSearch(){const params=new URLSearchParams(location.search),q=(params.get("search")||localStorage.getItem("farmDirectVoiceSearch")||"").trim();if(q){const input=document.getElementById("searchInput");if(input){input.value=q;localStorage.removeItem("farmDirectVoiceSearch");}}}
function addOrdersLink(){return;}
function escapeHtml(x){return String(x??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#039;"}[m]));}
function escapeAttr(x){return escapeHtml(x).replace(/`/g,"&#096;");}
